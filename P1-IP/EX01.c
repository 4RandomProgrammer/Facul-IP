#include <stdio.h>

int main() {    
    int n, i;

    do {
        scanf("___", &n);
    } while (________);

    for (i = _____; n > 1; i++)
        for (; ___________; ___________)
            printf(_______________);
    return 0;
}